Before 1st release
 * decimal properties
 * better look
 * remove menu (or add a reasonable one)
 * try uploading to stax
   - stax db config & drivers
   - sbt stax deploy task
 * edit only for logged in users
 * CSV import 

later
 * I18n
 * properties on model